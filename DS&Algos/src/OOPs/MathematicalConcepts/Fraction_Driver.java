package OOPs.MathematicalConcepts;

public class Fraction_Driver {
    public static void main(String[] args) {
        Fraction fraction = new Fraction(5, 0);
        fraction.printFraction("");
        // fraction.simplifyFraction();
        fraction.incrementFraction(1);
    }
}
